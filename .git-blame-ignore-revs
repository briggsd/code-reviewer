# Bulk mechanical commits to skip in git blame.
# Use: git blame --ignore-revs-file .git-blame-ignore-revs <file>
# Or once per clone: git config blame.ignoreRevsFile .git-blame-ignore-revs

# Biome formatter adoption + lint-debt clear, squash-merged as PR #107 (#96).
# NOTE: list the SQUASH commit on main, not the branch commit — squash merges
# rewrite the hash and a non-ancestry rev here is silently inert.
8cc2f4cc9d65e136e3ef0bd0b312a52eccaa7754
