{
  "runId": "eval-clean-refactor",
  "safetyMode": "trusted",
  "metadata": {
    "provider": "github",
    "repository": {
      "provider": "github",
      "owner": "example",
      "name": "app",
      "slug": "example/app",
      "defaultBranch": "main"
    },
    "changeId": "2",
    "headSha": "bbb001",
    "baseSha": "bbb000",
    "sourceBranch": "refactor/extract-constant",
    "targetBranch": "main",
    "title": "Extract MAX_RETRIES constant",
    "description": "Extracts the magic number 3 into a named constant MAX_RETRIES for clarity.",
    "author": {
      "username": "dev"
    },
    "labels": []
  },
  "diff": {
    "files": [
      {
        "path": "src/retry.ts",
        "status": "modified",
        "additions": 3,
        "deletions": 2,
        "isBinary": false,
        "patch": "@@ -1,6 +1,7 @@ import { sleep } from './utils';\n+const MAX_RETRIES = 3;\n \n export async function withRetry<T>(fn: () => Promise<T>): Promise<T> {\n   let attempt = 0;\n-  while (attempt < 3) {\n+  while (attempt < MAX_RETRIES) {\n     try {\n       return await fn();\n     } catch (err) {\n@@ -10,6 +11,6 @@ export async function withRetry<T>(fn: () => Promise<T>): Promise<T> {\n       attempt++;\n     }\n   }\n-  throw new Error(`Failed after 3 attempts`);\n+  throw new Error(`Failed after ${MAX_RETRIES} attempts`);\n }"
      }
    ],
    "totalAdditions": 3,
    "totalDeletions": 2,
    "truncated": false
  },
  "config": {
    "mode": "blocking",
    "failOn": ["critical"]
  }
}
