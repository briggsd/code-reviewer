{
  "runId": "eval-hardcoded-secret",
  "safetyMode": "trusted",
  "metadata": {
    "provider": "github",
    "repository": {
      "provider": "github",
      "owner": "example",
      "name": "app",
      "slug": "example/app",
      "defaultBranch": "main"
    },
    "changeId": "3",
    "headSha": "ccc001",
    "baseSha": "ccc000",
    "sourceBranch": "feature/api-client",
    "targetBranch": "main",
    "title": "Add API client configuration",
    "description": "Adds configuration for the external API client.",
    "author": {
      "username": "dev"
    },
    "labels": []
  },
  "diff": {
    "files": [
      {
        "path": "src/api-client.ts",
        "status": "modified",
        "additions": 5,
        "deletions": 1,
        "isBinary": false,
        "patch": "@@ -1,5 +1,9 @@ import { fetch } from 'node-fetch';\n+\n+const API_KEY = 'sk-prod-SYNTHETIC_TEST_KEY_DO_NOT_USE';\n+const BASE_URL = 'https://api.example.com/v1';\n \n export async function callApi(endpoint: string, data: unknown) {\n-  return fetch(`https://api.example.com/v1/${endpoint}`, { method: 'POST', body: JSON.stringify(data) });\n+  return fetch(`${BASE_URL}/${endpoint}`, {\n+    method: 'POST',\n+    headers: { 'Authorization': `Bearer ${API_KEY}` },\n+    body: JSON.stringify(data),\n+  });\n }"
      }
    ],
    "totalAdditions": 5,
    "totalDeletions": 1,
    "truncated": false
  },
  "config": {
    "mode": "blocking",
    "failOn": ["critical"]
  }
}
