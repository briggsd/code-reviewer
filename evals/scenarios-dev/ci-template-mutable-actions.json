{
  "name": "ci-template-mutable-actions",
  "description": "Primary reliability dimension: Convention memory. Precision guard for the settled factory convention that adopter CI templates may keep readable mutable action tags even though the factory's own workflows are SHA-pinned.",
  "fixture": "evals/fixtures/ci-template-mutable-actions.json",
  "threshold": 0.9,
  "criteria": [
    {
      "kind": "no_findings_at_or_above",
      "label": "no warning+ demanding SHA pinning in adopter templates",
      "severity": "warning"
    },
    {
      "kind": "decision_in",
      "label": "decision is approved or approved with comments",
      "values": ["approved", "approved_with_comments"]
    }
  ]
}
