node_modules/
.DS_Store
.env
.env.*
!.env.example
.ai-review/
.ai-review-smoke/
coverage/
dist/
telemetry-rollup.json

# Session-local working state — never tracked (handoff lives on the machine, not in history)
continue.md
